#include "searcher.h"

// Constructor
Searcher::Searcher(int id, int start, int finish, std::string word) 
: Id(id), Start(start), Finish(finish), Word(word) {} // atributos de la clase en mayusculas o minusculas

void Searcher::findOccurrences(){ // hay que poner Searcher::???

}